{
    "padding": {
        "insideBrackets": {
            "parens": "noSpace"
        },
        "beforeSemicolon": "noSpace",
        "afterClosingParen": "singleSpace"
    },
    "testProperties": {
        "entryPoint": "block_elements"
    }
}

---

function foo()call();
if (foo)call();
if ((foo))call();

---

function foo() call();
if (foo) call();
if ((foo)) call();