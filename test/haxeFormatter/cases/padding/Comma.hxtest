{
    "padding": {
        "comma": {
            "defaultPadding": "spaceAfter",
            "propertyAccess": "noSpaces"
        }
    },
    "testProperties": {
        "entryPoint": "class_fields"
    }
}

---

var a(default, null):Int = call(5,2);

---

var a(default,null):Int = call(5, 2);