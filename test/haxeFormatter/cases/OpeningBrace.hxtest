{
    "braces": {
        "newlineBeforeOpening": {
            "type": "no",
            "field": "no",
            "block": "no"
        },
        "newlineBeforeElse": "no"
    },
    "indent": {
        "whitespace": "    "
    },
    "testProperties": {
        "type": "invertible"
    }
}

---

class Main
{
    function foo()
    {
        if (foo)
        {
        }
        else
        {
        }

        if (foo)
            call()
        else
            call();
    }
}

---

class Main {
    function foo() {
        if (foo) {
        } else {
        }

        if (foo)
            call()
        else
            call();
    }
}