{
    "padding": {
        "insideBrackets": {
            "parens": "insert",
            "braces": "insert",
            "square": "insert",
            "angle": "insert"
        }
    },
    "testProperties": {
        "type": "invertible",
        "entryPoint": "block_elements"
    }
}

---

function foo<T>() {
    (50);
    [1, 2, 3];
    {a: "a", b: "b"};
    new Array<Int>();

    5 > 3;
    1 < 2;
}

---

function foo< T >( ) {
    ( 50 );
    [ 1, 2, 3 ];
    { a: "a", b: "b" };
    new Array< Int >( );

    5 > 3;
    1 < 2;
}