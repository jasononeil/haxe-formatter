{
    "indent": {
        "whitespace": "    ",
        "indentSwitches": true
    }
}

---

import foo;

class Main
{
function f() {
if (foo) {
trace("foo");
} else {
trace("bar");
}

if (foo)
{
    trace("foo");
}
else if (bar)
{
    trace("bar");
}
}

function g()
{
switch (foo)
{
case true:
call();
// comment
case false:
// comment
call();
default:
call();
}
}

function h()
{
if (foo)
call();
else
call();

if (foo)
if (foo)
call();

if (foo) if (foo)
call();

if (foo) if (foo) {
call();
}

for (i in 0...10)
trace(i);

while (true)
trace(0);

do
trace("")
while (true);

try
access.field()
catch (e:Any)
trace("kaputt");

function i()
trace("");

inline function i()
trace("");

#if true #end

#if true
        call();
#else
        call();
#end

trace(#if true true #else false #end);
trace("foo" /**/);

trace(
"a",
"b"
);
}

function i()
trace("i");

 // comment
// comment
/** comment **/
var a = [
1,
2,
3
];

#if foo @:noCompletion #end
function j(
a,
b,
c
)
{}

@:allow("foo")
function k() {}

function l() {
call([{
}]);

if (foo)
return;
}

function m() {
c +=
a +
b;

var c =
a +
b;
}

// comment before }
}

---

import foo;

class Main
{
    function f() {
        if (foo) {
            trace("foo");
        } else {
            trace("bar");
        }

        if (foo)
        {
            trace("foo");
        }
        else if (bar)
        {
            trace("bar");
        }
    }

    function g()
    {
        switch (foo)
        {
            case true:
                call();
            // comment
            case false:
                // comment
                call();
            default:
                call();
        }
    }

    function h()
    {
        if (foo)
            call();
        else
            call();

        if (foo)
            if (foo)
                call();

        if (foo) if (foo)
            call();

        if (foo) if (foo) {
            call();
        }

        for (i in 0...10)
            trace(i);

        while (true)
            trace(0);

        do
            trace("")
        while (true);

        try
            access.field()
        catch (e:Any)
            trace("kaputt");

        function i()
            trace("");

        inline function i()
            trace("");

        #if true #end

        #if true
        call();
        #else
        call();
        #end

        trace(#if true true #else false #end);
        trace("foo" /**/);

        trace(
            "a",
            "b"
        );
    }

    function i()
        trace("i");

    // comment
    // comment
    /** comment **/
    var a = [
        1,
        2,
        3
    ];

    #if foo @:noCompletion #end
    function j(
        a,
        b,
        c
    )
    {}

    @:allow("foo")
    function k() {}

    function l() {
        call([{
        }]);

        if (foo)
            return;
    }

    function m() {
        c +=
            a +
            b;

        var c =
            a +
            b;
    }

    // comment before }
}