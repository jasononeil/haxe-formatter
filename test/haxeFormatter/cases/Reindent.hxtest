{
    "indent": {
        "whitespace": "    ",
        "indentSwitches": true
    }
}

---

import foo;

class Main
{
function f() {
if (foo) {
trace("foo");
} else {
trace("bar");
}
}

function g()
{
switch (foo)
{
case true:
call();
// comment
case _:
// comment
call();
}
}

function h()
{
if (foo)
call();
else
call();

if (foo)
if (foo)
call();

for (i in 0...10)
trace(i);

while (true)
trace(0);

do
trace("")
while (true);

try
access.field()
catch (e:Any)
trace("kaputt");

function i()
trace("");

inline function i()
trace("");

#if true #end

#if true
        call();
#else
        call();
#end
}

function i()
trace("i");

 // comment
// comment
/** comment **/
var a = [
1,
2,
3
];

// comment before }
}

---

import foo;

class Main
{
    function f() {
        if (foo) {
            trace("foo");
        } else {
            trace("bar");
        }
    }

    function g()
    {
        switch (foo)
        {
            case true:
                call();
            // comment
            case _:
                // comment
                call();
        }
    }

    function h()
    {
        if (foo)
            call();
        else
            call();

        if (foo)
            if (foo)
                call();

        for (i in 0...10)
            trace(i);

        while (true)
            trace(0);

        do
            trace("")
        while (true);

        try
            access.field()
        catch (e:Any)
            trace("kaputt");

        function i()
            trace("");

        inline function i()
            trace("");

        #if true #end

        #if true
        call();
        #else
        call();
        #end
    }

    function i()
        trace("i");

    // comment
    // comment
    /** comment **/
    var a = [
        1,
        2,
        3
    ];

    // comment before }
}