{
    "indent": {
        "whitespace": "    "
    }
}

---

import foo;

class Main
{
function f() {
if (foo) {
trace("foo");
} else {
trace("bar");
}
}

function g()
{
switch (foo)
{
case true:
call();
case _:
call();
}
}
}

---

import foo;

class Main
{
    function f() {
        if (foo) {
            trace("foo");
        } else {
            trace("bar");
        }
    }

    function g()
    {
        switch (foo)
        {
            case true:
                call();
            case _:
                call();
        }
    }
}