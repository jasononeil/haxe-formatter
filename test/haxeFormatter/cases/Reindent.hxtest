{
    "indent": {
        "whitespace": "    ",
        "indentSwitches": true
    }
}

---

import foo;

class Main
{
function f() {
if (foo) {
trace("foo");
} else {
trace("bar");
}
}

function g()
{
switch (foo)
{
case true:
call();
case _:
call();
}
}

function h()
{
if (foo)
call();
else
call();
}
}

---

import foo;

class Main
{
    function f() {
        if (foo) {
            trace("foo");
        } else {
            trace("bar");
        }
    }

    function g()
    {
        switch (foo)
        {
            case true:
                call();
            case _:
                call();
        }
    }

    function h()
    {
        if (foo)
            call();
        else
            call();
    }
}